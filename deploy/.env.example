# ⚠️ 这是**首次部署**用的模板 —— 服务器上已有 .env 时请勿覆盖！
#    那里面是真实生产密钥，覆盖后本文件的 REPLACE_WITH_... 会顶替它们。占位符是**非空字符串**，
#    能通过 compose 所有 ${VAR:?} 必填检查，所以不会报错，只会表现为「部署成功但全线故障」：
#    MySQL 密码错 → backend 反复重启；CORS 来源是假 IP → 浏览器登录 403；MinIO 密钥错 → 上传失败。
#    重部署只追加新增项即可（本次为 AI_API_KEY / AI_ENABLED / AI_BASE_URL / AI_MODEL）。
#
# 首次部署：cp .env.example .env，然后逐项填真实值（可 grep REPLACE_WITH 自检）。
# 本模板对应的是**同一个目录下的 deploy/docker-compose.yml**（服务器上为 /opt/habitforge/deploy），
# 变量名以那份 compose 的 ${...} 为准。与仓库根目录的 docker-compose.yml（本机开发用，自带
# mysql/redis 容器、用 MYSQL_ROOT_PASSWORD）变量名**不同**，别照抄混淆。

# ══ 一、必填：不填 compose 直接拒绝启动并点名变量 ══════════════════════════════════

# JWT 签名密钥（强随机值，泄露 = 任何人可伪造登录态；更换会使所有已登录用户 token 失效）
# 生成方法：openssl rand -hex 32
JWT_SECRET=REPLACE_WITH_64_CHAR_RANDOM_HEX

# 服务器原生 MySQL 的密码。compose 里做的是 ${MYSQL_PASSWORD:?}，名字必须是 MYSQL_PASSWORD。
# 注意：不是 MYSQL_ROOT_PASSWORD —— 那个是仓库根目录那份 compose（自带 MySQL 容器）用的。
# 用户名另行通过 MYSQL_USER 覆盖，默认 root。
MYSQL_PASSWORD=REPLACE_WITH_REAL_MYSQL_PASSWORD
# MYSQL_USER=root

# CORS 允许的来源（逗号分隔）。compose 里是 ${APP_CORS_ALLOWED_ORIGIN_PATTERNS:?}，必填。
# 生产只放行精确来源；换域名/端口访问时必须同步改这里，否则浏览器 POST 会被 CORS 拒绝。
APP_CORS_ALLOWED_ORIGIN_PATTERNS=http://REPLACE_WITH_SERVER_IP:8081

# MinIO 对象存储（日记/笔记图片）的密钥
MINIO_SECRET_KEY=REPLACE_WITH_MINIO_SECRET_KEY

# ══ 二、必须人工决策：不填不报错，但对应功能**静默不可用** ════════════════════════
# 这一节最危险：留空时 compose 不拒绝启动、所有冒烟检查都通过，只有真正去用那个功能才报错。
# 部署核对时请逐条确认。

# AI 今日安排的密钥（DeepSeek，https://platform.deepseek.com 申请）。
# 只填在这里，严禁写进代码/配置/提交。
# 【留空 = AI 功能恒不可用】生成接口每次都返回 6001「AI 服务未启用」，而 /plans/today 等
# 其余接口与页面都正常，不填也能"部署成功"——但本次上线的核心功能等于没上。
# 故意留空而不是填占位符：占位符是非空字符串，会被当成"已配置"发出去撞 401，
# 表现为 6004「AI 服务调用失败」（看起来像服务坏了，其实是 key 假）；留空则明确是 6001。
AI_API_KEY=

# AI 总开关（默认 true）。本次不打算启用 AI 时，请**显式**设 false：
# 否则页面上会露出一个每次都报错的「AI 生成今日安排」入口；设 false 后不填 key 也行。
# AI_ENABLED=false

# Redis 密码：**仅当**给宿主机 Redis 设了 requirepass 时才填（当前服务器未设，留空即可）。
# 必须写在这里的原因：compose 只会把 environment 里显式列出的变量转发进容器，
# 变量名还必须一字不差（写成 REDIS_PASSWORD 就完全不生效），而应用启动阶段不探活 Redis，
# 漏写的表现是「部署成功、两个容器都 Up」，直到登出或 AI 生成才报 NOAUTH。
# SPRING_DATA_REDIS_PASSWORD=

# ══ 三、选填：compose 已给默认值，通常不用写 ══════════════════════════════════════

# MinIO 地址与 access key，默认 http://host.docker.internal:9000 / minioadmin。
# ⚠️ 改 MINIO_ENDPOINT 前必读：后端上传走这个值，但前端 `/images/` 的展示路径由
# deploy/nginx.conf 的 location /images/ **写死**指向 host.docker.internal:9000，
# 两边不一致会变成「上传 200、图片全部裂图」。改地址就得同步改 nginx。
# MINIO_ENDPOINT=http://host.docker.internal:9000
# MINIO_ACCESS_KEY=minioadmin

# 换供应商/换模型时的覆盖项（默认见 application.yml）。
# 这两项 compose 已转发，填在 .env 里即生效。
# AI_BASE_URL=https://api.deepseek.com/anthropic
# AI_MODEL=deepseek-v4-flash
